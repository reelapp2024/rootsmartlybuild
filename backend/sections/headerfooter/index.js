/**
 * Site-wide header/footer section definitions (prompts). Not page-scoped.
 */
const headerSection = require("./header/headerSection");
const footerSection = require("./footer/footerSection");

module.exports = {
  headerSection,
  footerSection,
};
