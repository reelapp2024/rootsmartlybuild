/**
 * Legacy contact/faq — prefer GenieBuild id `contactfaq`.
 * Kept for page-folder lookups when section id is still plain `faq` on Contact.
 */
module.exports = require("../contactfaq/contactfaqSection");
