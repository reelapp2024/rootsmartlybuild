/**
 * Content-site section prompt — legal / privacybody
 */

const {
  buildContentLegalBodySection,
} = require("../_contentLegalBodyPrompt");

module.exports = buildContentLegalBodySection("privacy", "privacybody");
