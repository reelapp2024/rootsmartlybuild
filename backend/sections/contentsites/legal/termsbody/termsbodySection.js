/**
 * Content-site section prompt — legal / termsbody
 */

const {
  buildContentLegalBodySection,
} = require("../_contentLegalBodyPrompt");

module.exports = buildContentLegalBodySection("terms", "termsbody");
