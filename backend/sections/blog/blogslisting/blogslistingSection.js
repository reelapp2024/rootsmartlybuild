/**
 * Legacy blob — maps to GenieBuild blogshero (+ search fields kept for older consumers).
 * Prefer generating `blogshero` / `blogssearch` separately.
 */
module.exports = require("../blogshero/blogsheroSection");
